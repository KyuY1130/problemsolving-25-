.append(x)
 리스트 끝에 x 를 첨부
.sort()
 리스트를 정렬 1,2,3.... or a,b,c...
.reverse()
 리스트를 역순
.index(x)
 리스트에 x 값이 있으면 그 인덱스값을 반환
.insert(a,b)
 a번째 인덱스에 b를 삽입
.remove(x)
 리스트에서 첫 번째로 나오는 x를 삭제
.count(x)
 리스트에서 x가 나오는 횟수
.extend(x)
 x에는 리스트만 올수 있으며 a.extend([4, 5])는 a += [4, 5]

슬라이싱
a=[1,2,3,4,5]
print(a[0])
print(a[1:])
print(a[:3])
print(a[2:4])

길이 
len(a)

리스트나 튜플은 순차적(sequential)